# Override the HTTP port (highest priority)
SERVER_HTTP_PORT=8080

# Or use the DEVHUB_ prefix for any nested setting:
# DEVHUB_AUTH__BASIC_AUTH__REGISTER_PASS=changeme
# DEVHUB_STORAGE__TYPE=json
# DEVHUB_STORAGE__JSON__PATH=/data/mcp_servers.json